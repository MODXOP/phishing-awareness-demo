services:
  - type: web
    name: phishing-awareness-app
    env: python
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: ADMIN_PASSWORD
        value: Utkarsh@8983
